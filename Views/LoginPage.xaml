<Page x:Class="TicTacToe.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:customControls="clr-namespace:TicTacToe.Views.CustomControls"
      mc:Ignorable="d"
      Title="LoginPage"
      Background="{StaticResource ButtonBackgroundBrush}">
    <Grid HorizontalAlignment="Center"
          VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <Image Source="/TicTacToe;component/Resources/GameName.png"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               Width="150"/>
        <TextBlock Text="{StaticResource StringAuth}"
                   Grid.Row="1"
                   FontSize="14"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Foreground="{StaticResource ForegroundBrush}"/>
        <customControls:CustomInput 
            Grid.Row="2"
            x:Name="UsernameTextBox" 
            Margin="10"
            Width="200"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            Placeholder="{StaticResource StringUsername}"/>
        <customControls:CustomPasswordBox 
            Grid.Row="3"
            x:Name="PasswordB"
            Margin="10" 
            Width="200"
            VerticalAlignment="Center"
            HorizontalAlignment="Center"
            PlaceholderText="{StaticResource StringPassword}"/>
        <customControls:CustomButton 
            Grid.Row="4" 
            Content="{StaticResource StringSignIn}" 
            Click="LoginButton_Click" 
            Margin="10"
            Width="150"
            Height="30"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"/>
        <customControls:CustomButton
            Grid.Row="5" 
            Content="{StaticResource StringSignUp}" 
            Click="GoToRegisterButton_Click" 
            Margin="10"
            Width="150"
            Height="30"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"/>
    </Grid>
</Page>
